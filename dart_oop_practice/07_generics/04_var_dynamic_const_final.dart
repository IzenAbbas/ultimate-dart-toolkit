/*

var => Data Type is determined automatically on compile time, once its is determined it cannot be changed.

dynamic => Any datatype is allowed even on run time, It can be changed afterwards.

final => Assigned only once on run time.

const => Assigned only once on compile time.
 */